public class Laptop {
    int processor;
    int ramSize;
    int hdSize;

    public Laptop(int processor, int ramSize, int hdSize) {
        this.processor = processor;
        this.ramSize = ramSize;
        this.hdSize = hdSize;
    }
}
